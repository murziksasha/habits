{
  "name": "@eduforge/mobile",
  "version": "0.0.1",
  "private": true,
  "main": "index.js",
  "scripts": {
    "start": "echo \"Install Expo deps: cd apps/mobile && npx expo start\"",
    "typecheck": "tsc --noEmit",
    "lint": "echo ok",
    "test": "vitest run",
    "build": "echo ok"
  },
  "devDependencies": {
    "typescript": "^5.8.3",
    "vitest": "^3.1.1"
  }
}

