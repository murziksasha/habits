# 62 — HTML Semantics deep track

## Model

- Course slug: **`html_semantics`**
- Separate from programming unit `html` (intro kept)
- Hub: `/html-semantics` · path: `/courses/html_semantics`
- Unit exams: `is_exam`, pass ≥ 0.7 (SPEC 60)

## Units

document → landmarks → heading-outline → text-media → lists-tables → forms → interactive → a11y-aria → capstone

## Content

`packages/content/src/html-semantics.ts`

## Migration

`0016_deep_tracks` — enum value `html_semantics`

## Related

- SPEC 23 programming HTML unit
- SPEC 60 unit exams
