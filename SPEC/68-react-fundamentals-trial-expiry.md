# 68 — React Fundamentals + trial expiry + audit UI

## Course `react_fundamentals`

- Units: intro, components, state, lists, effects, forms, composition, capstone
- Hub `/react-fundamentals`, exams ≥70%
- Achievement `react_fundamentals_start`
- Migration `0018_react_fundamentals`

## Trial expiry

On session load (`getUserFromToken`): if `plan=premium` and `planExpiresAt < now` → force `free`.

## Admin audit UI

- `/admin/audit` lists recent `admin_audit_log` rows
- Linked from admin header

## Related

- SPEC 67 JS + trial + audit table
