# 63 — CSS Layout (Flexbox + Grid)

## Model

- Course slug: **`css_layout`**
- Programming unit `css` remains short intro + mini
- Hub: `/css-layout` · path: `/courses/css_layout`

## Units

box-flow → flex-core → flex-items → flex-recipes → grid-core → grid-placement → grid-responsive → choose-tool → capstone

## Content

`packages/content/src/css-layout.ts` — heavy `code_fill` / `code_read` for properties; capstone `code_project`.

## Related

- SPEC 23 programming CSS
- SPEC 60 unit exams
